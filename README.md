# Milda

This repository contains a simple Snake game implemented in Python using `pygame`.

## Requirements

- Python 3.x
- `pygame` library (install via `pip install pygame`)

## Running the Game

Execute the following command from the repository root:

```bash
python3 snake_game.py
```

Use the arrow keys to control the snake. The game ends if the snake collides with itself.
